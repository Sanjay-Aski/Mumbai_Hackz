# FinSphere Environment Configuration - FULLY OFFLINE SETUP

# Backend Configuration (.env file for finsphere-backend/)
SECRET_KEY=your-secret-key-change-in-production

# 📡 Network Configuration (change these for multi-PC setup)
# For single PC: use localhost
# For network access: use PC's IP address (e.g., 192.168.1.25)
BACKEND_HOST=localhost
BACKEND_PORT=8000

# Qdrant Vector Database (Local/Network)
QDRANT_HOST=localhost
QDRANT_PORT=6333
QDRANT_COLLECTION_NAME=finsphere_context

# Ollama LLM Configuration (Local/Network)
OLLAMA_HOST=localhost
OLLAMA_PORT=11434
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=gpt-oss:20b-cloud
OLLAMA_EMBEDDINGS_MODEL=nomic-embed-text

# 📡 Network Setup Examples:
# Single PC setup (default):
# QDRANT_HOST=localhost
# OLLAMA_BASE_URL=http://localhost:11434

# Multi-PC setup (Qdrant on different PC):
# QDRANT_HOST=192.168.1.25
# OLLAMA_BASE_URL=http://192.168.1.30:11434

# Frontend Configuration (.env.local for finsphere-frontend/)
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1

# Extension Configuration (create extension/.env)
EXT_API_URL=http://localhost:8000/api/v1
EXT_FRONTEND_URL=http://localhost:3000

# Optional: Database (PostgreSQL - not required for offline setup)
# DATABASE_URL=postgresql://user:pass@localhost:5432/finsphere

# 🎯 NO API KEYS NEEDED! 
# ✅ Ollama runs completely locally/on network
# ✅ Qdrant runs completely locally/on network
# ✅ All data stays within your network

# 📡 Multi-PC Network Setup Instructions:
# 1. Find PC-A IP: ipconfig (e.g., 192.168.1.25)
# 2. Start Qdrant on PC-A: docker run -p 6333:6333 qdrant/qdrant
# 3. Set QDRANT_HOST=192.168.1.25 on other PCs
# 4. Open Windows Firewall port 6333 on PC-A
# 5. Test: ping 192.168.1.25 from PC-B

