# FinSphere Backend Environment Configuration

# Security
SECRET_KEY=your-super-secret-key-change-in-production

# 📡 Server Configuration
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000

# 📡 Network Configuration
# For single PC: use localhost
# For network access: use actual IP addresses
QDRANT_HOST=localhost
QDRANT_PORT=6333
QDRANT_COLLECTION_NAME=finsphere_context

# Ollama LLM Configuration
OLLAMA_HOST=localhost
OLLAMA_PORT=11434
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=gpt-oss:20b-cloud
OLLAMA_EMBEDDINGS_MODEL=nomic-embed-text

# 📡 Multi-PC Network Examples:
# If Qdrant runs on PC-A (IP: 192.168.1.25):
# QDRANT_HOST=192.168.1.25

# If Ollama runs on PC-B (IP: 192.168.1.30):
# OLLAMA_BASE_URL=http://192.168.1.30:11434

# 🔧 Performance Tuning
# Adjust based on your PC specs:
# OLLAMA_NUM_PREDICT=256  # Limit response tokens for speed
# QDRANT_BATCH_SIZE=100   # Vector upsert batch size

# Optional: Database (not required for basic functionality)
# DATABASE_URL=postgresql://user:pass@localhost:5432/finsphere

# 🚨 Important for Network Access:
# 1. Set BACKEND_HOST=0.0.0.0 to allow connections from other PCs
# 2. Open Windows Firewall port 8000 for backend access
# 3. Find your IP: ipconfig
# 4. Test from other PC: curl http://192.168.1.YOUR_IP:8000/api/v1/health