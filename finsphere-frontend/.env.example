# FinSphere Frontend Environment Configuration

# 📡 Backend API Configuration
# For single PC setup:
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1

# For network access (when backend runs on different PC):
# NEXT_PUBLIC_API_URL=http://192.168.1.25:8000/api/v1

# 📡 WebSocket Configuration (future use)
NEXT_PUBLIC_SOCKET_URL=ws://localhost:8000

# 📡 Network Examples:
# If backend runs on PC-A (IP: 192.168.1.25):
# NEXT_PUBLIC_API_URL=http://192.168.1.25:8000/api/v1
# NEXT_PUBLIC_SOCKET_URL=ws://192.168.1.25:8000

# 🔧 Development Configuration
NODE_ENV=development
NEXT_PUBLIC_APP_NAME=FinSphere
NEXT_PUBLIC_APP_VERSION=1.0.0

# 📊 Analytics (optional)
# NEXT_PUBLIC_ANALYTICS_ID=your-analytics-id

# 🎨 Theme Configuration
NEXT_PUBLIC_DEFAULT_THEME=light

# 🚨 Network Access Instructions:
# 1. Find backend PC IP: ipconfig
# 2. Update NEXT_PUBLIC_API_URL with backend PC's IP
# 3. Ensure backend allows connections (BACKEND_HOST=0.0.0.0)
# 4. Test API connection: fetch the /health endpoint
# 5. Frontend will be accessible at: http://YOUR_PC_IP:3000